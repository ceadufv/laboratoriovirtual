class NumberCalc {

    static getMax(val1, val2) {
        return Math.max(parseFloat(val1), parseFloat(val2));
    }

    static getMin(val1, val2) {
        return Math.min(parseFloat(val1), parseFloat(val2));
    }
}