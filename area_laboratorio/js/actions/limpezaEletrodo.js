/**
* @name	Ação teste
* @description Apenas uma função de teste
* @valid_source ["bequer"]
* @valid_target ["pisseta"]
* 
* @error {"sujo(bequer)" : "O béquer está sujo, lave-o primeiro"}
*/
function limpezaEletrodo(interacao) {
	console.log('Interacao')
	/*

	var source = interacao.source();
	var phmetro = interacao.target();

	var pHmetro = new LabPhmetro({ desvioPadrao:0.02 });
	pHmetro.solucao(source);

	interacao.target().data('pHmodo').text = 'pH';

	// Acopla ao pHmetro
	//phmetro.acoplarAopHmetro(source);

	// Realiza rotiza de medidcao de pH
	var tempo = 0;
	LabPhmetro._loop = function () {
		if (tempo >= 20) return;
		interacao.target().data('pHvisor').text = pHmetro.medirpH( tempo ).pHDisplay.toFixed(2)
		tempo ++;
	}
	*/
}